type Certificate = {
  id: string;
  certificateArn: string;
  certificateId: string;
  certificatePem: string;
  publicKey: string;
  privateKey: string;
  deviceID: string;
};

export default Certificate;
